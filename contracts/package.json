{
  "name": "@hpbrain/contracts",
  "version": "1.0.0",
  "description": "The single source of truth. Types are generated from schemas.",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "check": "npm run generate && git diff --exit-code dist/"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.0",
    "js-yaml": "^4.1.0"
  }
}
