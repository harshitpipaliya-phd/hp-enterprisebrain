# Used by docker-compose.yml. Copy to .env and set a real secret before running
# `docker-compose up` — the compose file refuses to start without one (see
# api/src/config.ts's production boot guard).
JWT_SECRET=replace-with-a-real-random-secret-at-least-32-characters
